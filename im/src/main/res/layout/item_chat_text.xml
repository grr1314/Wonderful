<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingHorizontal="20dp"
    android:paddingVertical="10dp"
    >

    <ImageView
        android:scaleType="centerCrop"
        android:visibility="gone"
        android:id="@+id/iv_user_icon_left"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:visibility="gone"
        android:id="@+id/tv_text_left"
        android:gravity="start"
        android:layout_marginStart="10dp"
        android:textSize="16sp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/iv_user_icon_left"
        app:layout_constraintTop_toTopOf="@id/iv_user_icon_left" />

    <ImageView
        android:id="@+id/iv_user_icon_right"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:scaleType="centerCrop"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:text="@string/app_name"
        android:id="@+id/tv_text_right"
        android:gravity="end"
        android:layout_marginEnd="10dp"
        android:textSize="16sp"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/iv_user_icon_right"
        app:layout_constraintTop_toTopOf="@id/iv_user_icon_right" />

</androidx.constraintlayout.widget.ConstraintLayout>